<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="AppTheme" parent="@style/Theme.Leanback" />

    <!-- 全局Dialog样式 -->
    <style name="BaseDialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:buttonBarStyle">@drawable/mainuser_linkgroup_background</item>
    </style>

    <style name="companyShowFontStyle" parent="@android:style/Theme">
        <item name="android:textSize">23sp</item>
        <item name="android:textColor">@color/matteOrange</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="companyShowFontStyle1" parent="@android:style/Theme">
        <item name="android:textSize">23sp</item>
        <item name="android:textColor">@color/white</item>
    </style>

</resources>
